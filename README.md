This web project is just a proof of concept so I can start building a new Python application from scratch.
It uses simple html, css, a small sqlite database (no real plans to add to it)
still a nice POC for development

